/**
 * 
 * handle shift when selecting group access
 *
 */
/**
 * 
 * Test if the input is JSON
 *
 */
function IsJsonString(t){try{JSON.parse(t)}catch(t){return!1}return!0}$("input").click(function(t){t.shiftKey&&
/* data-group="group name" */
$('[data-group="'+$(this).data("group")+'"]').prop("checked",$(this).prop("checked")||!1)}),
/**
 * 
 * handle shift in a tab group
 *
 */
$("div.tab-content :checkbox").click(function(t){t.shiftKey&&$("div.tab-pane.active :checkbox").prop("checked",$(this).prop("checked")||!1)}),
/**
 * 
 * attach to a field to link to another field (probably readonly) 
 * to create a computer safe value
 * <input class="js-human-input" data="computer-input"...
 * other options include:
 * data-always="true"
 * 
 * Note: on Post this is always filled in since it is a new record
 *
 */
$(".js-human-input").on("keyup blur",function(t){var e=$(this).data("always");"post"==$(this).closest("form").attr("method").toLowerCase()&&(e=!0),e&&
/* uses tool.js url_title function */
$("input[name='"+$(this).data("computer-input")+"']").val(url_title($(this).val()))}),
/**
 * 
 * add max length to textareas pre-html5
 * <textarea maxlength="255"...
 *
 */
$("textarea[maxlength]").bind("input propertychange",function(){var t=$(this).attr("maxlength");$(this).val().length>t&&$(this).val($(this).val().substring(0,t))}),
/**
 * 
 * this adds json syntax checking to textareas
 * <textarea class="js-validate-json"...
 *
 */
$(document).on("keyup focus",".js-validate-json",function(t){IsJsonString($(this).val())?
/* is good */
$(this).closest("div").removeClass("has-error"):
/* fall */
$(this).closest("div").addClass("has-error")});
//# sourceMappingURL=form_helpers.min.js.map