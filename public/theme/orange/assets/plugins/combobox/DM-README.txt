Because this library is used without modification I have included this additional readme

This is used in conjunction with to provide the dynamic lookups

Pear::combobox();

The plugin provides the on page code for attaching the <input id's and events.