/**
 *
 * <form action="/post.php" method="post"...
 *
 * data-confirm="heading|body|icon|cancel|ok"
 *		show a confirm dialog with the provided options before allowing the form to submit
 *		pipe separated
 *
 * data-primary_id="id" - default id
 *		This is the form element name of the primary id <input name="id" value="...
 *		This defaults to "id" but some models have "md5" or something else as the primary id
 * 		When this is set the input field is filled with the primary id returned from POST (insert) to make it a PATCH (update) 
 *
 * data-success="Text|style|(bool)stay" - stay defaults to false
 *		on success
 *			Text to display in dialog 
 *			dialog style
 *			should the dialog stay up
 *			pipe separated
 *			(note if redirect is included then this is stored and shown on the next page)
 *
 * data-redirect="/url"
 *		redirect to this url on success
 *		if this is "action" redirect to the forms action attribute value
 *
 * data-fadeout="tr"
 *		closest element to fade out on success
 *
 * ...>
 *
 *
 * if the page include the variable
 * 		orange_form_error_group = 'foobar';
 * 		orange_form_error_group = ['modelb','modela'];
 *
 * then rest form library will only listen to errors from those error groups (usually models)
 *
 */
/*
 * setup the objects if they don't already exist
 */
var orange=orange||{};orange.dialog=orange.dialog||{},orange.form=orange.form||{},
/**
 * 
 * handler for the submit button click
 *
 */
orange.dialog.button_submit_click=function(r,o){
/* is a confirm data attribute on the form element? */
if(
/* prevent the default action */
r.preventDefault(),
/*save a copy of "this" */
orange.that=$(o),
/* travel up from the submit button and find the closest <form> */
orange.form_obj=orange.that.closest("form"),orange.form_obj.data("confirm")){
/* these are a pipe separated list  (seemed the easiest - note the order) */
var e=1!=orange.form_obj.data("confirm")?orange.form_obj.data("confirm").split("|"):"",a=e[0]?e[0]:"Are you sure?",n=e[1]?e[1]:"",t=e[2]?e[2]:"exclamation-triangle",g=e[3]?e[3]:"Cancel",i=e[4]?e[4]:"Ok";
/* show the confirm dialog */
orange.dialog.show(t,a,n,g,i)}else
/* no dialog to show so just call the confirm dialogs "continue" button callback */
orange.dialog.confirm_ok(r,o)}
/**
 * 
 * when Form Ajax Fails Hard
 *
 */,orange.dialog.request_fail=function(){
/* pop up a fail message */
orange.flash_msg("Action Failed","red",!1)}
/**
 * 
 * when Form Ajax Success - but could have errors
 *
 */,orange.dialog.request_done=function(r){
/* show any errors */
if(
/* hide all of the notices */
$.noticeRemoveAll(),orange.form.has_error(r.ci_errors)){var o=orange.form.get_errors(r.ci_errors);for(var e in o)$.noticeAdd({text:o[e],type:"danger",stay:!0})}else{
/* no errors so continue with work */
/**
		 * 
		 * is fadeout set for this action highlight in the "info" color
		 * for a split second until we get lower in the code  where we fade it out
		 *
		 */
orange.that.closest(orange.form_obj.data("fadeout"))&&orange.that.closest(orange.form_obj.data("fadeout")).addClass("info")
/**
		 * 
		 * don't forget to merge the primary key hidden on the details form with the returned value from a insert
		 * get the primary key from the stored value on the form or default to id
		 *
		 */;var a=orange.form_obj.data("primary_id")?orange.form_obj.data("primary_id"):"id",n;
/**
		 * 
		 * did the responds have a primary key value? if so put that as the value
		 *
		 */
/**
		 * 
		 * does the form have a data-success attribute
		 * data-success="message|style|[false|action]"
		 *
		 */
if(r.primary_key&&$("input[name="+a+"]").val(r.primary_key),orange.form_obj.data("success")){var t=orange.form_obj.data("success").split("|"),n=!!orange.form_obj.data("redirect")||"false"!==t[2]&&orange.form_obj.attr("action");
/* does redirect contain something? if it does than save msg in browser local storage */orange.flash_msg(t[0],t[1],n),
/* switch post (insert) to patch (update) now */
"post"==orange.form_obj.attr("method").toLowerCase()&&orange.form_obj.attr("method","PATCH")}
/**
	 * 
	 * now fade out closet JQuery selector if data-fadeout="" is set
	 * of course if you are redirecting this doesn't matter
	 *
	 */
/**
	 * 
	 * redirect if a redirect exists
	 *
	 * options include a URL or "action" which redirect to the forms action attribute value.
	 *
	 */
if(orange.that.closest(orange.form_obj.data("fadeout"))&&orange.that.closest(orange.form_obj.data("fadeout")).fadeOut(),orange.form_obj.data("redirect"))"action"==(n=orange.form_obj.data("redirect"))&&(n=orange.form_obj.attr("action")),orange.redirect(n)}}
/**
 * 
 * dialog confirm button or form submit
 *
 */,orange.dialog.confirm_ok=function(){orange.dialog.request=$.ajax({method:orange.form_obj.attr("method"),url:orange.form_obj.attr("action"),data:orange.form_obj.serializeArray(),dataType:"json",cache:!1,timeout:5e3,async:!0}),orange.dialog.request.fail(function(){
/* defined above and can be overridden */
orange.dialog.request_fail()}),orange.dialog.request.done(function(r){void 0===r?
/* beats me what happened? Show a flash msg. */
orange.flash_msg("Unknown Reply","yellow",!1):
/* it's all good call the function */
/* defined above and can be overridden */
orange.dialog.request_done(r)})}
/**
 * 
 * main form submit button
 *
 */,$(".js-button-submit").on("click",function(r){orange.dialog.button_submit_click(r,this)}),
/**
 * 
 * generic form submit
 * these can be overwritten on the page to provide other actions
 *
 */
orange.form.success=function(r){var o=!0;
/* hide all of the notices */
/* show any errors */
if($.noticeRemoveAll(),orange.form.has_error(r.ci_errors)){var e=orange.form.get_errors(r.ci_errors);for(var a in e)$.noticeAdd({text:e[a],type:"danger",stay:!0});o=!1}return o}/* end orange.form.success */,orange.form.request_done=function(r,o){},/* place holder for user to extend as needed */
orange.form.submit=function(r,o,e){orange.form.request=$.ajax({method:r,url:o,data:e,dataType:"json",cache:!1,timeout:5e3,async:!0}),orange.form.request.fail(function(){
/* defined above and can be overridden */
orange.dialog.request_fail()}),orange.form.request.done(function(r){void 0===r?
/* beats me what happened? Show a flash msg. */
orange.flash_msg("Unknown Reply","yellow",!1):orange.form.request_done(r,orange.form.success(r))})}/* end orange.form.submit */,orange.form.has_error=function(r,o){return 0!=orange.form.get_errors(r,o,"").length},orange.form.get_errors=function(r,o,e){var a=[],n=!1;
/* return all error groups */
if(
/* only count and build from these error groups - if provided */
orange.form.error_group&&(o=orange_form_error_group),null==o)for(var t in n=!0,r)r.hasOwnProperty(t)&&(group=orange.form.get_error(r,t,e))&&a.push(group);
/* only return the error groups in this array */if(Array.isArray(o))for(var g in n=!0,o)(group=orange.form.get_error(r,g,e))&&a.push(group);
/* only return the error group in this string */return 0==n&&(group=orange.form.get_error(r,o,e))&&a.push(group),a},orange.form.get_error=function(r,o,e){e=e||"<br>";var a=!1;return Array.isArray(r[o])&&0<r[o].length&&(a=r[o].join(e)),a};
//# sourceMappingURL=rest-form.min.js.map